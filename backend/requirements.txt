# Production API dependencies. Keep these exact so local, CI, and serverless
# builds run the same tested stack. Training-only packages live under scripts/.
fastapi==0.141.1
uvicorn==0.52.1
pydantic==2.13.4
numpy==2.5.1
scikit-learn==1.9.0
